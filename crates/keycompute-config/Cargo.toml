[package]
name = "keycompute-config"
version = "0.1.0"
edition = "2024"

[dependencies]
config = "0.14"
serde = { version = "1.0", features = ["derive"] }
thiserror = "1.0"
tracing = "0.1"

[dev-dependencies]
tokio = { version = "1.0", features = ["full"] }
